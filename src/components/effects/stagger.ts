/**
 * หน่วงเวลาปรากฏของรายการลำดับที่ i ในชุดเดียวกัน — **ใช้ตัวนี้ทุกที่ อย่าคูณเอง**
 *
 * ทำไมต้องมีเพดาน: หน้า /research มี 72 แถว ถ้าหน่วงต่อเนื่องแถวละ 60ms
 * แถวสุดท้ายจะรอกว่า 4 วินาทีกว่าจะโผล่ ซึ่งผู้อ่านจะอ่านว่าเว็บค้าง ไม่ใช่เว็บสวย
 * หน่วงแค่ 6 ตัวแรกก็พอให้รู้สึกเป็นระลอก ที่เหลือมาพร้อมกัน
 *
 * **อยู่แยกไฟล์จาก `Reveal.tsx` โดยตั้งใจ** — `Reveal` เป็น `"use client"`
 * ทุก export ในไฟล์นั้นจึงกลายเป็นฝั่ง client และ **server component เรียกไม่ได้**
 * (build พังจริงตอนรวมไว้ไฟล์เดียว) ไฟล์นี้ไม่มี `"use client"` ทั้งสองฝั่งจึงใช้ได้
 */
export const stagger = (i: number) => Math.min(i, 5) * 60;
