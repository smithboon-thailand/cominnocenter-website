# CLAUDE.md — ComInno Center Website

คู่มือสำหรับ Claude Code ในโปรเจ็ค redesign เว็บ ComInno Center
(ศูนย์เชี่ยวชาญเฉพาะทางด้านนวัตกรรมการสื่อสารฯ คณะนิเทศศาสตร์ จุฬาลงกรณ์มหาวิทยาลัย)

> **เอกสารคู่กัน:** `BRAND.md` ที่ root คือ source of truth ด้านดีไซน์ทั้งหมด
> แผนงาน Phase 0–5 อยู่ใน `cominno-workflow.md` (เอกสารในแชทหลัก — สรุปย่ออยู่ท้ายไฟล์นี้)
> **เว็บขึ้น production แล้วตั้งแต่ 16 ส.ค. 2569** งานปัจจุบันดูหัวข้อ "งานหลัง cutover" ท้ายไฟล์

---

## Tech stack

| ส่วน | เทคโนโลยี |
|---|---|
| Framework | Next.js 15 (App Router) + React 19 + TypeScript 5 |
| Styling | Tailwind CSS 3.4 (config: `tailwind.config.ts`) + CSS variables ใน `src/app/globals.css` |
| ฟอนต์ | **Kanit** ตัวเดียวทั้งเว็บ ผ่าน `next/font/google` — weights **400/500 เท่านั้น** (BRAND.md v1.2: ห้าม 300/600/700 · display/h1 = 500 two-tone · h2 = 400) |
| Utilities | clsx, tailwind-merge |
| Deploy | Vercel — ทุก branch ที่ push ได้ Preview URL อัตโนมัติ |
| รูปภาพ | **self-host แล้ว** (Phase 0-B) ใน `public/images/` — 320 ไฟล์ (246 ไฟล์จาก Wix ดู mapping ใน `scripts/wix-image-manifest.json` + ภาพ paper-craft จาก Grok) · เหลือ remote เฉพาะรูปหลักสูตรวัฒนธรรมจาก `cuculturecom-static.vercel.app` |
| SEO/analytics | JSON-LD ทุกหน้าผ่าน `src/lib/schema.ts` · `sitemap.ts` · hreflang TH/EN · Vercel Analytics ใน root layout |
| Favicon | `src/app/icon.png` + `apple-icon.png` — ทำจากโลโก้ปัจจุบัน **ใช้ถาวร** (ผู้ใช้ยืนยันคงโลโก้เดิม 16 ส.ค. 2569) |
| Security header | `next.config.ts` ส่งสี่ตัวให้ทุก path (PR #32) — `X-Content-Type-Options` · `Referrer-Policy` · `X-Frame-Options` · `Permissions-Policy` · **CSP** (PR #44) ล็อก `connect-src`/`img-src`/`frame-src` ไว้เฉพาะโดเมนที่เว็บเรียกจริง + `object-src 'none'` · `base-uri 'self'` · `frame-ancestors` — **ถ้าเพิ่มบริการภายนอกใหม่ต้องมาเพิ่มที่นี่ ไม่งั้นมันจะเงียบไป** แล้วรัน `npm run check:csp` · HSTS มาจาก Vercel เอง **ห้ามแตะ** (`includeSubDomains` + `preload` ถอนคืนใช้เวลาเป็นเดือน) |

คำสั่งหลัก: `npm run dev` · **`npm run verify`** (lint + build + ตรวจข้อความที่ผู้อ่านเห็น + ตรวจไฟล์ภาพครบทุกขนาด + ตรวจรายการอ้างอิงทั้งสี่รูปแบบ + ตรวจเส้นทาง/ลิงก์/sitemap/redirect + ตรวจค่าที่เลิกใช้/ตัวตนของโดเมน/ที่เว้นว่าง — ใช้ตัวนี้ก่อน push) · `npm run build` · `npm run lint` (ESLint 9 flat config ที่ `eslint.config.mjs` — ต่อยอด `next/core-web-vitals` + `next/typescript`) · `npm run check:text` · `npm run check:images` (สองตัวนี้ต้อง build ไว้ก่อน) · `npm run check:citations` (ตรวจจากข้อมูลตรงๆ ไม่ต้อง build · ใส่ `-- --print` เพื่อพิมพ์ APA/MLA ทุกรายการออกมาอ่านเอง) · `npm run check:routes` · `npm run check:content` (สองตัวนี้ต้อง build ก่อน) · `npm run check:csp` (**รันมือ ไม่อยู่ใน verify** — ต้อง build ก่อนและต้องมี playwright: `npm i --no-save playwright`)

## โครงสร้างโฟลเดอร์

```
src/
├── app/                    # App Router — root layout สองตัว ดูหมายเหตุใต้ผัง
│   ├── (th)/               # ภาษาไทย — วงเล็บเป็น route group ไม่มีผลกับ URL
│   │   ├── layout.tsx      # root layout ไทย (lang="th", og:locale th_TH, ฟอนต์, JSON-LD)
│   │   ├── page.tsx        # Home (ไทย)
│   │   ├── about/  expertise/  collaborate/
│   │   ├── impact/         # รายการโครงการ + impact/[slug]/ รายละเอียด
│   │   ├── news/           # ข่าวที่เก็บจากเว็บเดิม + news/[slug]/ (Phase 0-C)
│   │   ├── media/          # "สื่อถึงเรา" — ศูนย์ฯ ปรากฏบนสื่อภายนอก
│   │   ├── research/       # ผลงานวิชาการ (จาก publications.ts)
│   │   ├── sdg/            # 17 เป้าหมาย + วงล้อ SDG
│   │   ├── not-found.tsx   # หน้า 404 ไทย · [...notFound]/ ดึง URL แปลกๆ มาที่นี่
│   ├── (en)/               # ภาษาอังกฤษ — โครงสร้างขนานกันครบทุกหน้า
│   │   ├── layout.tsx      # root layout อังกฤษ (lang="en", og:locale en_US)
│   │   └── en/             # page.tsx about/ expertise/ collaborate/ media/ research/ sdg/
│   │                       #   impact/ impact/[slug]/ news/ news/[slug]/
│   │                       #   not-found.tsx + [...notFound]/
│   ├── globals.css         # design tokens ตาม BRAND.md I1 + keyframes
│   ├── sitemap.ts          # สร้างจาก data file แตกสองภาษาอัตโนมัติ + hreflang
├── components/
│   ├── Header  Footer  ContactForm  NewsletterForm  ProjectGallery  VideoShowcase
│   ├── ui/                 # Button, DisplayHeading, PageHero, ProjectCard, ResponsiveArtwork,
│   │                       #   SdgBadge, SdgFilterChips, SdgPosterGrid, SectionHeader,
│   │                       #   SectionIcon (ไอคอน 20 บทบาท), Stat
│   ├── effects/            # HeroArtwork, Reveal, ParallaxHero, GlassCard, AnimatedCounter,
│   │                       #   InnovationNetwork
│   ├── impact/             # ImpactExplorer (filter), ProjectFooterNav (related/prev-next)
│   ├── expertise/          # ExpertiseExplorer (4 ช่วงกระบวนการ), ServiceIcon
│   ├── research/           # ResearchExplorer      · media/ MediaExplorer
│   ├── sdg/SdgWheel        # search/SiteSearch (⌘K) · seo/JsonLd · about/TeamAndPartners
├── lib/                    # ตรรกะที่ไม่ผูกกับ UI (ดูตารางด้านล่าง)
└── data/                   # ข้อมูลทั้งหมดของเว็บ (ดูตารางด้านล่าง)
public/
├── images/                 # ภาพ self-host ทั้งหมด (projects/ news/ media/ expertise/ home/ og/)
├── illustrations/          # SVG ประกอบ (hero-network, sdg-ring, ฯลฯ)
└── robots.txt
```

**ทำไม root layout ถึงมีสองตัว (29 ส.ค. 2569)** — `<html lang>` ต้องบอกภาษาของหน้านั้นจริงๆ (WCAG 3.1.1) แต่ layout เป็น server component ที่อ่าน pathname ไม่ได้ ถ้าจะอ่านต้องพึ่ง `headers()` ซึ่งจะดึงทั้งเว็บออกจาก static ไปเป็น dynamic ทั้ง 102 หน้า วิธีที่ Next รองรับคือ root layout หลายตัวผ่าน route group จึงแยกเป็น `(th)` กับ `(en)` — **วงเล็บไม่มีผลกับ URL** `/about` ยังเป็น `/about` เหมือนเดิม

ผลที่ตามมาที่ต้องจำ:
- **เพิ่มหน้าใหม่ต้องวางใต้ group ให้ถูก** ไทยไป `(th)/` อังกฤษไป `(en)/en/` ไม่งั้นจะได้ `lang` ผิดภาษา
- `metadata` ของหน้าลูก**ทับ**ของ layout ทีละ field ระดับบนสุด ไม่ merge เข้าไปข้างใน — เขียน `openGraph` ของตัวเองเมื่อไหร่ ต้องใส่ `images` ด้วยทุกครั้ง ไม่งั้นหน้านั้นจะไม่มีภาพตอนแชร์
- Next render `not-found` ในเปลือก `<html id="__next_error__">` ของตัวเอง ไม่ใช่เปลือกของ layout เรา ฟอนต์/พื้นหลังจึงอยู่ใน `not-found.tsx` เอง อย่าย้ายกลับไปพึ่ง layout

## Data files (`src/data/`)

| ไฟล์ | เนื้อหา |
|---|---|
| `projects.ts` | โครงการทั้งหมด 18 โครงการ (17 โครงการมี gallery ใน `projectMedia.ts`) — field `sdg: SdgId[]` แล้ว (Phase 1-C) ตัวแรก = สีหลักของการ์ด · `sourceUrl` 17 รายการชี้ `/news/<slug>` ภายใน (Phase 0-C) |
| `sdg.ts` | SDG 17 เป้าหมาย × 3 stops ตาม BRAND.md I2 + `SDG_WHITE_TEXT_OK` + `sdgAria()` · **เกณฑ์ map SDG ให้โครงการ: อิงผลลัพธ์ปลายทางของโครงการ ไม่ใช่รูปแบบกิจกรรม ใส่เป้าหมายรองเฉพาะที่เนื้องานรองรับจริง** |
| `news.ts` | ข่าว 24 โพสต์ที่เก็บจากเว็บเดิม (ไทย+อังกฤษ) — `sourceUrl` เก็บ URL Wix เดิมไว้ทำ 301 redirect ใน Phase 0-D |
| `newsMedia.ts` | (generated) local path ของภาพในโพสต์ข่าว — สร้างจาก `scripts/wix-posts-manifest.json` |
| `projectMedia.ts` | mapping slug → media id ของภาพ gallery (id ใช้เป็นชื่อไฟล์ local ใน `public/images/projects/<slug>/` แล้ว) |
| `projectCopyEn.ts` | คำแปลอังกฤษของเนื้อหาโครงการ |
| `services.ts` | บริการ 9 ด้าน + 4 ช่วงกระบวนการ (`ServiceStage`) ที่หน้า Expertise ใช้จัดเรียง · `projectSlugs` ผูกบริการกับโครงการจริง |
| `media.ts` | "สื่อถึงเรา" 22 รายการที่ศูนย์ฯ ปรากฏบนสื่อภายนอก — field `code` ตรงกับชื่อไฟล์ thumbnail `public/images/media/C-XX.webp` |
| `publications.ts` | **(generated — ห้ามแก้ด้วยมือ)** ผลงานวิชาการ 75 รายการ · field `verified` 4 ระดับ `doi`/`link`/`index`/`self` (47/2/11/15) · สร้างด้วย `scripts/fetch-publications.mjs` · **ถ้าสคริปต์แจ้งว่ามีผลงานหาย ให้ตรวจก่อนเสมอ** แล้วค่อยสั่ง `--allow-removals` |
| `paperSummaries.ts` | บทสรุปย่อยง่ายไทย+อังกฤษของผลงานที่ทำหน้า `/research/[slug]` — **45 ชิ้นจาก 75** · **`kind` มีสองค่าสำหรับงานที่ไม่มีผลเชิงประจักษ์** — `"protocol"` ประกาศแผนวิจัย 2 ชิ้น (หัวข้อเป็น "จะทำอะไร") และ `"argument"` บทความเชิงแนวคิด 4 ชิ้น (หัวข้อเป็น "เสนออะไร" และป้ายบอกด้วยว่า**ข้อเสนอเป็นของผู้เขียน ไม่ใช่จุดยืนของศูนย์ฯ**) · ทั้งสองค่าเปลี่ยนหน้าเว็บ **สี่ที่** ต้องแก้ครบ: หัวข้อใน `PaperSummaryBody` · ป้ายเหนือเนื้อหา · คำนำหน้าชื่อเรื่องบนหน้า detail ทั้งสองภาษา · คำบนลิงก์ใน `ResearchExplorer`) · ผูกกับ `publications.ts` ด้วย `doi` **หรือ `indexUrl`** เพราะวารสารไทยใน ThaiJO (`jprad`, `jcomm`, `jcmn`) **ไม่ออก DOI ให้บทความเลย** เขียนเป็น union type บังคับให้มีอย่างใดอย่างหนึ่ง และ `publicationForSummary()` จะโยน error ถ้าเชื่อมไม่ติด (ให้ build พังดีกว่าปล่อยหน้ากำพร้าที่อยู่ใน sitemap แต่ขึ้น 404) · **`license` ไม่มีค่า = วารสารสงวนลิขสิทธิ์ ไม่ใช่ยังไม่ได้ตรวจ** ห้ามเดาใส่ และหน้าเว็บต้องไม่แสดงข้อความ CC ให้รายการที่ไม่มีค่า · `pdfUrl` ชี้ไปคลังของวารสารต้นทาง (เปิดแท็บใหม่) **เว็บไม่เก็บไฟล์ PDF เอง** · `localCopy` คือสำเนาใน `research-sources/papers/` ไว้ให้ AI อ่าน ไม่ได้เสิร์ฟ — **ใส่ได้เฉพาะ Creative Commons เพราะคลัง GitHub นี้เป็นสาธารณะ** ดู `research-sources/README.md` · **เน้นข้อความด้วย `**...**` ได้** `PaperSummaryBody` แปลงให้เป็นน้ำหนัก 500 (ห้ามใช้ `<strong>` เปล่าๆ เพราะค่าเริ่มต้นเบราว์เซอร์คือ 700 ผิด BRAND.md) · **ห้ามระบุชื่อองค์กรหรือบุคคลที่ถูกวิจารณ์ในข้อค้นพบ** (กติกาข้อ 5 ในหัวไฟล์) บทความต้นฉบับระบุได้เพราะเป็นงานวิชาการ แต่บทสรุปบนเว็บเป็นงานเขียนของศูนย์ฯ เอง ผู้อ่านย่อมอ่านว่าเป็นคำตัดสินของศูนย์ฯ · ต้องตัดตัวเลขที่ชี้ตัวได้ออกด้วย · ทุกรายการต้องมี `illustrationAltTh/En` และภาพสามขนาดใน `public/images/research/summaries/` |
| `paperVideos.ts` | **วิดีโอเล่าสาระหลักของบทความ** — slug → id YouTube ไทย/อังกฤษ + ความยาว + วันอัปโหลด · หน้า `/research/[slug]` สลับภาพประกอบหัวหน้าเป็นตัวเล่นวิดีโอ (`PaperVideo` — facade โหลด iframe เมื่อกดเล่น หน้าปกคือภาพประจำบทความในเว็บเรา ไม่ดึงจาก YouTube) และประกาศ `VideoObject` ใน JSON-LD เฉพาะหน้าที่มีคลิป · **คลิปต้องเป็น public หรือ unlisted** private ฝังไม่ได้ · `slug` ต้องตรงกับ `paperSummaries.ts` (โยน error ตอนโหลด + `check:content` ตรวจซ้ำ) · ไม่มี `youtubeId` = ยังไม่อัปโหลดคลิปภาษานั้น หน้าแสดงภาพประกอบตามปกติ |
| `leadership.ts` | ผู้บริหารศูนย์ |
| `team.ts` / `researchers.ts` | ทีมงานและนักวิจัย |
| `partners.ts` | หน่วยงานพันธมิตร |
| `highlights.ts` | ข่าว/ไฮไลต์กิจกรรม |
| `videos.ts` | วิดีโอ showcase |
| `illustrations.ts` | path ของ SVG ใน `public/illustrations/` |
| `pageBanners.ts` | **แหล่งความจริงเดียวของภาพประกอบหัวหน้าหลัก 7 หน้า** (about · expertise · impact · research · news · media · collaborate) — path + alt ไทย/อังกฤษ · หน้าไทยกับหน้าอังกฤษเป็นคนละไฟล์ ถ้าให้แต่ละไฟล์เขียน path เองจะเพี้ยนออกจากกัน จึงบังคับให้อ่านจากที่นี่ผ่าน `<PageHero page="…" locale="…" />` (เดิมชื่อ `PageBanner` — ลบไปแล้วใน PR #34) · **ทุกใบต้องมีสามขนาด** 800/1200/1600 เพราะ `unoptimized: true` แปลว่า Next ไม่สร้าง srcset ให้ ถ้ามีแต่ 1600 มือถือจะโหลดไฟล์เต็มทั้งที่ช่องภาพกว้าง 340px และภาพนี้เป็น LCP element ของหน้า · **field `focus` คือจุดครอปแนวนอน วัดจากจุดศูนย์ถ่วงของพิกเซลที่อิ่มสี ไม่ได้กะด้วยตา** — `PageHero` ครอปภาพ 21:9 ลงช่อง 4:3 ค่าที่กะเองจะดันวัตถุไปติดขอบเฟรม (about 97% · expertise 98% · impact 81% · research 84% · news 91% · media 94% · collaborate 53%) |
| `contact.ts` / `social.ts` | **แหล่งความจริงเดียวของข้อมูลติดต่อ** — เบอร์โทร (4 รูปแบบจัดจากเลขชุดเดียว) · **อีเมล `comm.inno@chula.ac.th` (มีจุดคั่น)** · และลิงก์โซเชียล · ทั้ง Footer, ContactForm, /collaborate, /privacy-policy, JSON-LD และ /llms.txt อ่านจากที่นี่ **ห้ามพิมพ์ค่าซ้ำในหน้าใดหน้าหนึ่ง** — เคยหลุดตรงกันมาแล้วทั้งสามเรื่อง (อีเมลหนักสุด: พิมพ์ผิดพร้อมกันทั้ง 9 จุด เมลที่ส่งมาจึงเด้งกลับโดยศูนย์ฯ ไม่รู้ตัว) |

ภาพทั้งหมด self-host แล้ว (Phase 0-B) — ฟังก์ชัน `media()/logo()` ในแต่ละ data file ชี้ไป `public/images/` · สคริปต์ดาวน์โหลด: `scripts/download-wix-images.mjs` (ต้องรันด้วย `NODE_USE_ENV_PROXY=1` ในเซสชัน remote)

## Logic files (`src/lib/`)

| ไฟล์ | หน้าที่ |
|---|---|
| `schema.ts` | JSON-LD ทั้งเว็บ — `organizationSchema`, `websiteSchema`, `personSchema`, `publicationListSchema`, `breadcrumbSchema`, `newsArticleSchema` · **`publicationListSchema` กรอง `verified === "self"` ออก** ไม่ยืนยันสิ่งที่พิสูจน์ไม่ได้ต่อ Google |
| `citation.ts` | สร้างรายการอ้างอิง APA 7 · MLA 9 · BibTeX · RIS ตอน build · **ใช้ `citation.authors` เท่านั้น ห้ามใช้ `authors`** ซึ่งเก็บแต่คนของศูนย์ฯ ถ้าใช้ผิดผู้ร่วมวิจัยจะหายจากเครดิต · BibTeX หนีอักขระ LaTeX แล้ว (`&` ในชื่อวารสารทำให้คอมไพล์ไม่ผ่าน) · **คืนค่าเป็น `CitationSegment[]` ที่บอกว่าชิ้นไหนต้องเป็นตัวเอน** เพราะตัวเอนคือส่วนหนึ่งของรูปแบบ ไม่ใช่การตกแต่ง (ชื่อวารสาร เลขเล่ม ชื่อหนังสือ) หน้าเว็บ render เป็น `<em>` ส่วนปุ่มคัดลอกส่งข้อความล้วน · **แปลงตัวพิมพ์ชื่อเรื่องให้ตรงมาตรฐาน** APA ใช้ sentence case · MLA ใช้ title case โดยมีตาราง `PROTECTED_WORDS`/`PROTECTED_PHRASES` กันวิสามานยนาม (Thailand, YouTube, AI, Generation Z ฯลฯ) และไม่แตะคำที่มีตัวใหญ่กลางคำ (HIV, VTubers, COVID, U.S.) · **เลขบทความต่างจากเลขหน้า** APA ใส่ `Article` นำหน้า · MLA ห้ามใส่ `pp.` · หนังสือใน MLA เป็นตัวเอนไม่มีเครื่องหมายคำพูด |
| `related.ts` | `relatedProjects(slug)` ให้คะแนนจากข้อมูลจริง (+3 บริการร่วม · +2 SDG หลักเดียวกัน · +1 ต่อ SDG รองที่ซ้ำ) เสมอกันตัดสินด้วย slug เพื่อให้ build เสถียร · `projectNeighbours(slug)` ทำ prev/next แบบวน |
| `searchIndex.ts` | ดัชนีค้นหาสร้างตอน build จาก data files (โครงการ ข่าว สื่อ บริการ ผลงานวิชาการ SDG 17 ข้อ และหน้าหลัก) — ไม่มี backend ไม่มีไลบรารีค้นหา |
| `text.ts` | `truncate(text, max, locale)` ตัดข้อความตาม**ขอบเขตคำจริง** ด้วย `Intl.Segmenter` (Node 22 มี ICU เต็ม) — **ใช้กับทุก `description` ห้ามใช้ `.slice()`** เพราะภาษาไทยไม่มีช่องว่างระหว่างคำ ตัดตามจำนวนอักขระจะขาดกลางคำ ("การเปิดร" แทน "การเปิดรับ") และไม่มีใครเห็นเพราะ `description` ไม่ได้อยู่บนหน้าเว็บ — ไปโผล่ในผลการค้นหาและการ์ดตอนแชร์ · เคยหลุด **56 จาก 70 หน้า** (PR #33) · `check:content` ฟ้องถ้าเจอ `description:` คู่กับ `.slice(` |

## Scripts (`scripts/`)

| ไฟล์ | หน้าที่ |
|---|---|
| `fetch-publications.mjs` | ดึงและ**ตรวจสอบ**ผลงานวิชาการ แล้วเขียนทับ `src/data/publications.ts` — ORCID + Crossref + doi.org CSL + ดัชนีอิสระ + ตาราง `THAIJO_SOURCES` ที่ตรวจซ้ำ `citation_author` ทุกครั้งที่รัน · **แคชคำตอบของทะเบียนไว้ใน `.cache/` (ไม่ขึ้นคลัง อายุ 7 วัน)** เพราะรอบ 2 ก.ย. Crossref ปัด 429 ทุกรายการติดกันจนรันไม่จบ พอมีแคชแล้วรันซ้ำจะยิงเฉพาะที่ยังขาด · สั่ง `--no-cache` เพื่อยิงสดทั้งหมด · **คัดชื่อหน่วยงานที่ทะเบียนลงผิดช่องผู้เขียนออก** · แยก `articleNumber` ออกจาก `page` · ตาราง `TITLE_FIXES`/`CITATION_FIXES` แก้เฉพาะช่องที่ทะเบียนลงไม่ครบ **โดยอ้างหลักฐานจากตัวไฟล์บทความ และต้องเขียนคอมเมนต์บอกที่มาทุกแถว** |
| `download-wix-images.mjs` · `download-wix-posts.mjs` | สคริปต์ย้ายภาพ/โพสต์จาก Wix (Phase 0 — เก็บไว้อ้างอิง) |
| `indexnow.mjs` | แจ้ง Bing ว่ามีหน้าใหม่/หน้าที่แก้ไข (`npm run indexnow -- <url>`) — ดูหัวข้อ IndexNow ท้ายไฟล์ |
| `check-rendered-text.mjs` | อ่านข้อความที่ผู้อ่านเห็นจริงในหน้าที่ build ออกมา แล้วมองหามาร์กอัปดิบที่ไม่ควรหลุด (`npm run check:text` หรือรวมใน `npm run verify`) — ดูกฎการทำงานข้อ 6 |
| `check-image-files.mjs` | อ่าน `srcset` จาก HTML ที่ build ออกมา แล้วตรวจว่าไฟล์ภาพทุกไฟล์มีอยู่จริงใน `public/` (`npm run check:images`) — **ภาพหนึ่งใบต้องมีสามขนาด ถ้าไฟล์ย่อหาย ภาพจะพังเฉพาะเครื่องที่ความหนาแน่นพิกเซลตรงกับไฟล์นั้น เดสก์ท็อปยังเห็นปกติ คนตรวจจึงไม่มีทางเจอ** |
| `check-citations.ts` | สร้างการอ้างอิงทั้ง 4 รูปแบบของผลงานทุกชิ้นแล้วตรวจตามกฎ (`npm run check:citations` · `-- --print` เพื่ออ่านเอง) — เลขบทความต้องมี `Article` ใน APA และไม่มี `pp.` ใน MLA · `p.` กับหน้าเดียว `pp.` กับช่วงหน้า · ต้องมีส่วนตัวเอน · หนังสือไม่ใส่เครื่องหมายคำพูด · BibTeX ปีกกาสมดุลและไม่มี `&` ดิบ · RIS เปิด `TY` ปิด `ER` · **เรียกฟังก์ชันจริงใน `citation.ts` ไม่เขียนตรรกะซ้ำ** ไม่งั้นตัวตรวจกับตัวจริงจะเพี้ยนออกจากกัน · **มีเพราะรายการอ้างอิงที่ผิดหลุดขึ้นเว็บจริง และผู้ใช้เป็นคนพบจากการเอาไปให้ผู้ตรวจอิสระดู** |
| `check-content.ts` | ตรวจ**เนื้อหา**ที่ขึ้นเว็บจริง (`npm run check:content` · ต้อง build ก่อน) — ย้ายมาจาก `check-content.portable.mjs` ของคลัง `comminno-web` ที่กำลังเลิกใช้ · ตรวจสี่เรื่อง: **(1) ค่าที่เลิกใช้แล้ว** อีเมลแบบไม่มีจุด · เบอร์ 02-218-2262 เดิม · โฮสต์ภาพของ Wix (บัญชี Wix จะถูกยกเลิกกลางเดือน ก.ย. ภาพที่ยังชี้ไปที่นั่นจะหายทันที) · สำเนาเว็บที่ปิดไปแล้ว · `localhost` — **ใส่ค่าเก่าเข้าลิสต์ทุกครั้งที่แก้ค่าใดค่าหนึ่ง** นั่นคือสิ่งที่กันไม่ให้บั๊กที่แก้แล้วกลับมาอีกหกเดือนถัดไป · **(2) ตัวตนของโดเมน** canonical · hreflang · og:image · `@id` ใน JSON-LD · `<loc>` ใน sitemap · บรรทัด `Sitemap:` ใน robots.txt ต้องชี้ `www.cominnocenter.com` ทั้งหมด (ตรวจเฉพาะช่องที่*ประกาศว่าอะไรคือฉบับจริงของหน้า* ไม่ตรวจลิงก์ออกทั่วไป) · **(3) ที่เว้นว่าง**ในหน้าที่มีผลทางกฎหมาย — ประกาศที่ยังมีวงเล็บว่างแย่กว่าไม่มีประกาศ เพราะเชิญให้คนส่งคำขอมาที่ที่ไม่มีใครรับ · **(3ข) ลิงก์ที่ไม่มีข้อความข้างใน** `<a href="…"></a>` กินพื้นที่ศูนย์พิกเซล ผู้อ่านมองไม่เห็นและกดไม่ได้ เท่ากับหน้านั้นไม่มีลิงก์นั้นอยู่จริง แม้ href จะถูกทุกตัวอักษร (ยกเว้นลิงก์ที่มี `aria-label`/`title` หรือห่อไอคอนไว้) · **(4) การอ้างถึงกันเองของข้อมูล** `projectMedia` · `projectCopyEn` · `services[].projectSlugs` · `projects[].sourceUrl` ล้วนเป็น `string` ที่ TypeScript ตรวจให้ไม่ได้ พิมพ์ผิดแล้ว**ไม่มีอะไรพัง** แค่แกลเลอรีว่าง/หน้าอังกฤษไม่มีคำแปล/การ์ดหลักฐานหายไปเงียบๆ · **อ่านไฟล์ข้อมูลเป็นข้อความ ไม่ได้ import** เพราะไฟล์ในคลังนี้อ้างกันด้วย `@/` ซึ่ง Node ตามไม่ได้ (เหตุผลเดียวกับ `check-citations.ts`) จึงบังคับให้ทุกการดึงประกาศจำนวนขั้นต่ำไว้ และถือว่าดึงได้น้อยกว่านั้นคือ**สคริปต์เสีย** ไม่ใช่ข้อมูลว่าง — ป้องกันการ "ผ่านเพราะไม่ได้ตรวจอะไรเลย" |
| `check-csp.mjs` | ตรวจ **Content-Security-Policy ด้วยเบราว์เซอร์จริง** (`npm run check:csp` · ต้อง build ก่อน · ต้องมี playwright: `npm i --no-save playwright`) — ยก `next start` เอง เปิดหกหน้าที่มี embed/ฟอร์ม แล้วดัก `securitypolicyviolation` ที่เบราว์เซอร์ยิงออกมา ซึ่งเป็น**ตัวชี้ขาด** ไม่ใช่การเดาจากรายการโดเมน · กดเล่นวิดีโอ YouTube ด้วย เพราะ facade สร้าง iframe ตอนกดเท่านั้น ไม่กดก็ไม่ได้ทดสอบ `frame-src` · **จงใจไม่อยู่ใน `npm run verify`** เพราะต้องยกเซิร์ฟเวอร์และเปิด Chromium ซึ่งช้ากว่าตัวตรวจอื่นมาก · ให้รันเมื่อ**แก้ CSP หรือเพิ่มบริการภายนอกใหม่** เท่านั้น · **วิธีอ่านผล: คำขอที่ล้มเหลวเพราะเน็ตไม่ใช่ CSP** — ถ้าเป็น CSP จะมี violation ขึ้นคู่กันเสมอ (ตอนเขียนได้ทดสอบเชิงลบด้วยการตัดสามโดเมนออกแล้วดูว่า violation โผล่ตรงตำแหน่งที่ควรจริง) |
| `check-routes.ts` | ตรวจว่า**เส้นทาง**ของเว็บยังต่อกันครบ (`npm run check:routes`) — ลิงก์ภายในทุกเส้นไปถึงหน้าที่มีจริงหรือมี redirect รองรับ · sitemap ตรงกับหน้าที่ build ออกมาทั้งสองทาง · **กฎ redirect ไม่บังหน้าจริง** (บั๊กที่เคยเกิดกับ `/privacy-policy` — redirect ทำงานก่อน routing หน้าใหม่จึงไม่มีวันถูกเรียกถึง) ไม่ต่อกันเป็นทอด และไม่วนหาตัวเอง · หน้าไทยกับอังกฤษมีครบเป็นคู่ · อ่านจากผลลัพธ์ที่ build ออกมา **ไม่ต้องยกเซิร์ฟเวอร์** · รายการ route ที่ไม่ใช่ HTML (`/llms.txt`, `/icon.png`) อ่านจากไฟล์ `.body` ที่ Next สร้าง ไม่ได้พิมพ์ไว้ตายตัวจึงไม่ล้าสมัยเงียบๆ |
| `video/` | **ตัวประกอบวิดีโอเล่าสาระหลักของบทความ** (Python · ดู README ในโฟลเดอร์) — `storyboard.py` แหล่งความจริงของบท/ข้อความบนจอ/แนวคิดภาพ · `build_v2.py` เรนเดอร์เฟรม + ตัดบรรทัดไทยตามวรรค · `build_v3.py` ประกอบเสียงทีละย่อหน้า + จิงเกิล/เพลง/SFX + มาสเตอร์ · `credits.py` ชื่อผู้เขียนทุกคนบนการ์ดปิดจาก `citation.authors` · `check_wrap2.py` ตรวจการตัดบรรทัดทุกฉากก่อนประกอบ · **ไฟล์สื่อไม่อยู่ในคลัง** (เสียงโคลนของผู้ใช้ + คลังสาธารณะ) · กติกาอยู่หัวข้อ "วิดีโอเล่าสาระหลักของบทความ" ท้ายไฟล์นี้ |

## กติกา i18n

- **ไทย = ภาษาหลัก** อยู่ที่ root path (`/`, `/about`, `/impact/...`)
- **อังกฤษ** อยู่ใต้ `/en` (`/en`, `/en/about`, `/en/impact/...`) โครงสร้างต้องขนานกับไทยเสมอ
- เพิ่มหน้าใหม่ต้องทำทั้งคู่ เช่น `/sdg` คู่กับ `/en/sdg`, `/news/[slug]` คู่กับ `/en/news/[slug]`
- เนื้อหาหน้าไทยต้องไทยล้วน หน้าอังกฤษต้องอังกฤษล้วน — ห้ามปนภาษาในประโยคเดียว ยกเว้นชื่อเฉพาะ (ตาม BRAND.md PART C/F)

## กฎการทำงาน (บังคับทุกครั้ง)

1. **ทุกการตัดสินใจด้านดีไซน์ต้องปฏิบัติตาม `BRAND.md`** ถ้าโค้ดเดิมขัดแย้ง ให้ BRAND.md ชนะ (เช่น สีชมพูเดิม `#DE5C8E` ใน tailwind.config.ts ต้องแทนด้วย `pink-500 #E0218A`, ฟอนต์ Sarabun/Inter ต้องแทนด้วย Kanit)
2. **ห้าม push ตรงเข้า `main`** — ตั้งแต่ 16 ส.ค. 2569 `main` คือเว็บ production จริง (โดเมน www.cominnocenter.com ชี้ Vercel แล้ว) ทุกการเปลี่ยนแปลงทำบน branch `claude/*` ของเซสชัน → เปิด PR → ให้ผู้ใช้ตรวจ Preview/อนุมัติก่อน merge เสมอ (branch `feature/redesign` จบภารกิจแล้ว — merged เข้า main ไปแล้ว ไม่ใช้ต่อ)
3. **Phase 0–5 จบหมดแล้ว** งานหลังจากนี้ทำเป็นฟีเจอร์ต่อฟีเจอร์ หนึ่งเรื่องหนึ่ง PR ไม่ต้องอิงลำดับ Phase อีก
4. **commit ย่อยบ่อยๆ** ขึ้นต้นด้วยขอบเขตงาน เช่น `research: `, `fix: `, `feat: `
5. **ก่อนแก้ไฟล์ใด อ่านไฟล์เต็มก่อนเสมอ**
6. **หลังแก้เสร็จ รัน `npm run verify` ให้ผ่านก่อน push ทุกครั้ง** (ไม่ใช่แค่ `build`)
   `verify` = lint + build + `check:text` + `check:images` + `check:citations` + `check:routes`
   + `check:content` ซึ่งอ่าน
   **ผลลัพธ์ที่ผู้อ่านได้จริง** — `check:text` มองหามาร์กอัปดิบที่ไม่ควรหลุด (`**` แบบมาร์กดาวน์ ·
   ลิงก์ `[x](y)` · entity ซ้อนสองชั้น · แท็ก HTML ที่กลายเป็นข้อความ) · `check:images`
   ตรวจว่าไฟล์ภาพใน `srcset` มีครบทุกขนาด · `check:citations` ตรวจรายการอ้างอิงทั้งสี่รูปแบบ
   ตามกฎของ APA 7 และ MLA 9 · `check:routes` ตรวจว่าลิงก์ภายในทุกเส้นไปถึงหน้าจริง
   sitemap ตรงกับหน้าที่ build ออกมาทั้งสองทาง กฎ redirect ไม่บังหน้าจริงและไม่ต่อกันเป็นทอด
   และหน้าไทยกับอังกฤษมีครบเป็นคู่ · `check:content` ตรวจ**ค่าที่เลิกใช้แล้ว** (อีเมลแบบไม่มีจุด ·
   เบอร์เก่า · โฮสต์ Wix · สำเนาเว็บที่ปิดไปแล้ว) **ตัวตนของโดเมน** (canonical · hreflang ·
   og:image · `@id` · sitemap · robots.txt ต้องชี้โดเมนจริงทั้งหมด) **ที่เว้นว่าง**ในหน้าที่มีผล
   ทางกฎหมาย และ**การอ้างถึงกันเองของข้อมูล** ที่ TypeScript ตรวจให้ไม่ได้
   **มีเพราะบั๊กชนิดนี้หลุดขึ้นโปรดักชันมาแล้วสามครั้ง และทั้งสามครั้งผู้ใช้เป็นคนพบ**
   (`&amp;` ในชื่อวารสาร 30 ส.ค. · `**` ในบทสรุป 22 หน้า 1 ก.ย. · รูปแบบการอ้างอิงผิด
   ห้าเรื่อง 2 ก.ย. ซึ่งผู้ใช้เอาไปให้ผู้ตรวจอิสระดูแล้วเจอ) รากปัญหาเดียวกันคือ
   เราตรวจ*โครงสร้าง* (JSON-LD, metadata, ลิงก์) ละเอียด แต่ไม่เคยอ่าน*ตัวข้อความ*ที่ออกมา
   **จงใจไม่ผูกเข้ากับ `npm run build`** เพราะ Vercel รัน build ตอน deploy ถ้าสคริปต์พลาด
   จะทำให้เว็บจริง deploy ไม่ได้ ซึ่งร้ายแรงกว่าบั๊กที่มันกัน
7. **เนื้อหาไทยต้องไทยล้วน อังกฤษต้องอังกฤษล้วน**
8. **ห้ามขึ้นเว็บด้วยข้อมูลวิชาการที่ยังไม่ได้ตรวจ** — ผลงานวิชาการทุกรายการต้องพิสูจน์ได้ทั้งว่า *มีจริง* และ *เป็นของคนนั้นจริง* ก่อนแสดง ห้ามกรอกมือลง `publications.ts` ให้แก้ที่ `scripts/fetch-publications.mjs` แล้วรันใหม่ · รายการที่ยังพิสูจน์ออนไลน์ไม่ได้ (`self`) แสดงได้แต่ต้องกำกับที่มาให้ผู้อ่านเห็น และห้ามส่งเข้า JSON-LD

## สรุปแผนงาน (รายละเอียดเต็มใน cominno-workflow.md)

- **Phase 0 — Asset Independence: ✅ เสร็จสมบูรณ์** — 0-A สำรวจ · 0-B self-host ภาพ 246 ไฟล์ + favicon ชั่วคราว · 0-C เก็บ 24 โพสต์เป็น `/news/[slug]` + `/en/news/[slug]` (+39 ภาพ) · 0-D redirects 33 กฎใน `next.config.ts` (โพสต์เดิม 24 + /insights /contact /blog ฯลฯ + catch-all `/post/:slug*`) · 0-E ตรวจแล้ว runtime เหลือ dependency ภายนอกแค่ `cuculturecom-static.vercel.app` (รูป 7), YouTube (thumbnail+embed) และ `formspree.io` (backend ฟอร์มติดต่อ/newsletter — บริการเดิมของเว็บ)
- **Phase 1 — Design System: ✅ เสร็จสมบูรณ์** — tokens + Kanit 400/500 (typography v1.2 locked) + `src/data/sdg.ts` + components 6 ตัว + migrate `projects.ts` เป็น `sdg: SdgId[]` (mapping อนุมัติแล้ว)
- **Phase 2 — Assets จาก Grok: ✅ ภาพนิ่งครบทุกชุด (16 ส.ค. 2569)** — โลโก้เดิมใช้ถาวร · สไตล์ "Premium paper-craft" (วัตถุเดียว/2 สีต่อภาพ/คู่สีตามหมวด) เจนโดย Grok agent ผ่าน branch `grok/visual-assets` → `assets-inbox/` → Claude คัด/แปลง WebP: B ภาพ OG (`public/images/og/og-default.jpg` ประกอบโลโก้แล้ว) · C thumbnail 22/22 (`public/images/media/C-XX.webp` — รหัสตรง field `code` ใน `media.ts`) · D hero (`public/images/home/hero-bg.webp`) · E expertise 4 ภาพ (`public/images/expertise/`) · F collaborate banner · G 404 · H พื้นหลัง CTA · **ค้างเฉพาะวิดีโอ D+H** (hero ใช้ภาพนิ่ง + CSS motion แทนแล้วใน PR #11 — ดูหัวข้อ "งานหลัง cutover") · บทเรียน: การอัปโหลดไฟล์จำนวนมากผ่าน GitHub API ควรรวม commit ต่อชุด เพราะทุก commit กินโควตา deployment ของ Vercel แม้ตั้งข้าม build
- **Phase 3 — Implementation: ✅ เสร็จสมบูรณ์** — Impact list (filter ?sdg=N) → Impact detail (หนึ่งหน้าหนึ่งสี) → `/sdg` → Home → About/Expertise/Collaborate → language sweep → EN parity ครบ
- **Phase 4 — QA ทั้งเว็บ: ✅ เสร็จสมบูรณ์** — crawl 98 หน้า: ลิงก์ภายใน/alt/heading/h1/metadata/OG/hreflang ผ่านหมด · เพิ่ม `<main>` landmark ทุกหน้า · แก้การ์ด EN ที่โชว์ outcome ไทย · `/impact` เปลี่ยนเป็น static (filter ฝั่ง client ผ่าน `ImpactExplorer` — metadata อยู่ใน `<head>`) · Vercel Analytics ใน root layout · ลบ `/dev/components` · Lighthouse (home+impact, mobile+desktop) ทุกหมวด ≥ 93 · external links 26/34 ตอบ 200 ที่เหลือเป็น bot-block (FB/IG/LinkedIn/ResearchGate/Scopus) + TLS chain ไม่ครบของ `ee.eng.chula.ac.th` (เปิดในเบราว์เซอร์ได้ปกติ) · หมายเหตุ: contrast ของ pink-500/ink-500 บนพื้นอ่อนต่ำกว่า AA เล็กน้อย (4.06–4.41) — เป็นสีตาม BRAND.md ถ้าจะแก้ต้องหารือก่อน
- **Phase 5 — Cutover: ✅ เสร็จ 16 ส.ค. 2569** — merge PR #1 เข้า `main` + ย้ายโดเมน `www.cominnocenter.com`/apex มา Vercel แล้ว (SSL ผ่าน · apex 308→www · redirects เก่า 33/33 ผ่าน · crawl 98 หน้าบนโดเมนจริงผ่านหมด) · **MX อีเมล Google Workspace อยู่บน Wix DNS — ห้ามแตะ** · ค้าง: `en.cominnocenter.com` ยังชี้ Wix (404) รอผู้ใช้เพิ่มโดเมนใน Vercel เป็น redirect + แก้ CNAME · คงบัญชี Wix ถึง ~กลางเดือนกันยายน 2569 แล้วยกเลิกเฉพาะ website plan (**ห้ามยกเลิกส่วนโดเมน/DNS ก่อนย้าย DNS ออก**) 

## งานหลัง cutover (ทำเสร็จแล้ว — เรียงตามลำดับที่ขึ้นเว็บ)

| งาน | PR | สาระ |
|---|---|---|
| "สื่อถึงเรา" | #2 | `/media` + `/en/media` 22 รายการ + thumbnail ชุด C จาก Grok |
| วงล้อ SDG (ทาง A) | #3 | `SdgWheel` บน `/sdg` เลือกซี่แล้วเลื่อนไปการ์ดเป้าหมายนั้น (`:target` ไฮไลต์) |
| Expertise ใหม่ (ทาง A + C) | #6 | เรียง 9 บริการตาม 4 ช่วงกระบวนการ + การ์ดหลักฐาน + ไอคอน duotone 9 ตัว + แบนเนอร์ชุด S |
| แก้บั๊กมือถือ + Stat spec | #7–8 | ภาพการ์ดขอบขาว/ครอปเสีย → เปลี่ยนไปภาพ landscape จริง · `Stat` นับเลขไต่ขึ้นตาม spec ผู้ใช้ |
| ผลงานวิชาการ + structured data | #9 | `/research` 72 รายการพร้อมระดับการตรวจสอบ · JSON-LD ทั้งเว็บ |
| ค้นหา ⌘K + โครงการเกี่ยวข้อง | #10 | `SiteSearch` (ไม่มี backend) · `ProjectFooterNav` · BreadcrumbList |
| Hero มีชีวิต + header 768px | #11 | `HeroArtwork` (drift 26s + parallax, ปิดตาม reduced-motion) · แก้เมนูอังกฤษล้นจอ |
| เบอร์โทรศัพท์ใหม่ | #22 | รวมเบอร์ไว้ที่ `contact.ts` แหล่งเดียว 4 รูปแบบสร้างจากเลขชุดเดียว (เดิมกระจาย 7 จุด 3 รูปแบบ จึงเคยหลุดไม่ตรงกัน) |
| หน้าบทสรุปงานวิจัย 6 ชิ้น | #23 | `/research/[slug]` TH+EN + `paperSummaries.ts` · กู้ผลงานที่หายจากการแบ่งหน้า Crossref 4 ชิ้น |
| ปุ่มอ้างอิง 4 รูปแบบ | #24 | `CitationTool` (APA 7 · MLA 9 · BibTeX · RIS) สร้างตอน build จาก `citation.ts` · แก้ metadata ที่ทะเบียนสลับ field มา |
| บทสรุปเพิ่มจาก ThaiJO 9 ชิ้น | #25 | รวมเป็น 15 ชิ้น · รองรับงานที่**ไม่มี DOI** ด้วยกุญแจ `indexUrl` · `license` เป็น optional เพราะวารสารไทยส่วนใหญ่ไม่ใช่ CC |
| แก้ `**` หลุดขึ้นหน้าเว็บ | #26 | `withEmphasis()` แปลงมาร์กดาวน์เป็นการเน้นจริง 22 หน้า · `plainText()` กัน title/OG/breadcrumb |
| ภาพประกอบบทสรุปงานวิจัย | #30 | ภาพ paper-craft 15 ใบ หัวหน้า `/research/[slug]` TH+EN · **หลักการ: วาดรูปร่างของข้อค้นพบ ไม่ใช่วาดเนื้อเรื่อง** เพราะชุดนี้มีงานที่แตะเหตุการณ์จริงที่มีผู้เสียชีวิต การคุกคามทางเพศ ความขัดแย้งที่ยังดำเนินอยู่ และโรคที่ถูกตีตรา · เปลี่ยนภาพแชร์จาก `og-default.jpg` เป็นภาพประจำเรื่อง (เดิม 15 หน้าขึ้นการ์ดเหมือนกันหมด) · ยก srcset ออกมาเป็น `ResponsiveArtwork` ใช้ร่วมกับ `PageBanner` · เพิ่ม `check:images` · **ตัดชื่อสถานีโทรทัศน์ในบทสรุปข่าวโคราชตามที่ผู้ใช้ทัก** |
| แบนเนอร์โหลดตามขนาดจอ | #29 | วัดแล้วพบว่าแบนเนอร์เป็น LCP element ของ 12 หน้า แต่มือถือ (ช่องภาพ 340px) โหลดไฟล์ 1600px เต็ม เพราะ `unoptimized: true` ทำให้ `next/image` ไม่สร้าง srcset · เปลี่ยน `PageBanner` เป็น `<img>` + srcset สามขนาด (800/1200/1600) · มือถือ DPR2 ลด 73% DPR3 ลด 51% วัดจาก network ด้วย Playwright |
| ภาพประกอบหัวหน้าหลัก (ชุด A) | #28 | แบนเนอร์ paper-craft 6 ใบใหม่ (about · expertise · impact · research · news · media) เจนด้วย ElevenLabs โดยป้อน `collaborate/banner.webp` เป็นภาพอ้างอิงสไตล์ทุกใบ · รวม markup ไว้ที่ `PageBanner` + `pageBanners.ts` แล้วย้าย `/collaborate` มาใช้ตัวเดียวกัน · แก้ BRAND.md E3 ที่ยังเขียนว่า "line illustration สีเดียว" ให้ตรงกับ paper-craft ที่ใช้จริง |
| บทสรุปงานวิจัย open access 3 ชิ้น | #31 | เริ่มใช้ไฟล์บทความที่ผู้ใช้ส่งมา 20 ชิ้น · **ตัดงานสองชิ้นออกกลางทางเพราะอ่านแล้วพบว่าเป็น study protocol** ซึ่งเติมหัวข้อ "พบอะไร" อย่างซื่อสัตย์ไม่ได้ (ภายหลังทำเป็นหน้าแบบแผนวิจัยแทน ดูแถวถัดไป) |
| บทสรุปอีก 9 ชิ้น + ประกาศแผนวิจัย 2 ชิ้น | #31 | รวมเป็น 29 จาก 75 · เพิ่ม `kind: "protocol"` ให้หน้าแผนวิจัยเรียกหัวข้อว่า "จะทำอะไร" และขึ้นป้ายบอกว่ายังไม่มีผล — **ตามที่ผู้ใช้เสนอเมื่อ 2 ก.ย. ว่าแผนวิจัยก็ควรมีหน้าของตัวเอง เพียงแต่ต้องเรียกให้ตรงกับสิ่งที่มันเป็น** · ภาพประกอบ paper-craft อีก 7 ใบ |
| แก้รูปแบบการอ้างอิงทั้งระบบ | #31 | ผู้ใช้เอาผลลัพธ์ไปให้ผู้ตรวจอิสระดูแล้วพบข้อผิดพลาดห้าเรื่อง — เลขบทความถูกใช้เป็นเลขหน้า · ชื่อวารสารไม่เป็นตัวเอน · ตัวพิมพ์ชื่อเรื่องไม่ตรงมาตรฐาน (APA sentence case / MLA title case) · หนังสือถูกจัดรูปเหมือนบทความ · ชื่อหน่วยงานปนอยู่ในช่องผู้เขียน · แก้ทั้งที่ `citation.ts` และ `fetch-publications.mjs` แล้วเพิ่ม `check:citations` เข้า `npm run verify` |
| เติมข้อมูลอ้างอิงที่ขาด | #31 | ตรวจการอ้างอิงเทียบกับ**ตัวไฟล์บทความที่ตีพิมพ์** ไม่ใช่แค่กับทะเบียน แล้วพบว่าสามรายการมี DOI แต่ไม่มีข้อมูลบรรณานุกรมเลย จึงไม่มีปุ่มอ้างอิงบนเว็บ (หนึ่งในนั้นเป็นงานที่เพิ่งทำหน้าบทสรุปให้) · รากปัญหาคือ `findInIndexes` บันทึกแต่เลข DOI ไม่ได้ดึงระเบียนเต็ม · และพบเลขฉบับ/เลขหน้าที่หายไปอีกสามรายการ เติมจากบรรทัด "Cite this article as" ที่วารสารพิมพ์ไว้ในไฟล์เอง · `check:citations` ตรวจข้อนี้แล้ว โดย**ทำให้พังเฉพาะรายการที่มีหน้าบทสรุป** ซึ่งเป็นจุดที่ผู้อ่านเห็นความขาดจริง |
| แก้อีเมลของศูนย์ฯ ที่ผิดทั้งเว็บ | #31 | ผู้ใช้ยืนยันว่าที่อยู่ถูกต้องคือ `comm.inno@chula.ac.th` **แบบมีจุดคั่น** ขณะที่เว็บพิมพ์ `comminno@` ไว้ **เก้าจุด** — Footer · ContactForm ทั้งสองภาษา · /collaborate ทั้งสองภาษา · หน้านโยบายทั้งสองภาษา (สองจุดต่อหน้า) · JSON-LD · /llms.txt · ย้ายมาไว้ที่ `contact.ts` เป็นค่าเดียวเหมือนที่เคยทำกับเบอร์โทร · **อีเมลผิดร้ายแรงกว่าเบอร์ผิด** เพราะเมลเด้งกลับไปหาผู้ส่ง ศูนย์ฯ ไม่มีทางรู้ว่ามีใครติดต่อมาแล้วติดต่อไม่ได้ |
| บทสรุปงานวิจัยอีก 2 ชิ้น | #31 | รวมเป็น 31 จาก 75 — กรอบการนำเสนอในสื่อรัสเซีย (ไม่ระบุชื่อสำนักข่าวและบุคคลตามกติกาข้อ 5) · เสรีภาพในการแสดงออกกับโฆษณาเหยียดสีผิว (ไม่ระบุชื่อแบรนด์และไม่ยกถ้อยคำบนโฆษณา เพราะทั้งคู่ชี้ตัวบริษัทได้ทันที) · ชิ้นหลังส่วนใหญ่เป็น**ข้อโต้แย้งเชิงบรรทัดฐาน** จึงเขียนกำกับไว้ทั้งในข้อค้นพบและใน `caveat` ว่าเป็นข้อเสนอของผู้เขียน ไม่ใช่จุดยืนของศูนย์ฯ · เติมเลขหน้าที่ทะเบียนไม่ได้ลงไว้จากบรรทัด "Citation:" ในตัวไฟล์บทความ (**DOI ที่พิมพ์อยู่ในไฟล์ resolve ไม่ได้** ตัวที่จดจริงเป็นอีกรูปหนึ่ง) |
| ตาข่ายตรวจเส้นทาง `check:routes` | #31 | เพิ่มตามข้อเสนอจากการตรวจของอีกทีม · ตรวจลิงก์ภายใน · sitemap สองทาง · redirect ที่บังหน้าจริง/ต่อกันเป็นทอด/วนหาตัวเอง · คู่ไทย-อังกฤษ · **ทดสอบด้วยการใส่บั๊กทั้งสามชนิดเข้าไปจริงแล้วดูว่าสคริปต์จับได้** ก่อนจะเชื่อว่ามันทำงาน · ตอนเขียนพบว่าเว็บปัจจุบันผ่านทุกข้อ — มีไว้กันครั้งหน้า ไม่ใช่แก้ของที่พังอยู่ |
| บทสรุปบทความเชิงแนวคิด 4 ชิ้น + `kind: "argument"` | #31 | ครบทุกชิ้นที่มีไฟล์บทความแล้ว (35 จาก 75) — เสรีภาพในการแสดงออกกับสิทธิในทรัพย์สินของแพลตฟอร์ม · ความรับผิดเมื่อข่าวแจกเท็จผ่านกองบรรณาธิการ · ภาพพักหน้าจอในฐานะวิดีโออาร์ต · การสื่อสารกับการรับกฎศีลธรรม · **ทั้งสี่ชิ้นเป็นข้อเสนอเชิงบรรทัดฐานที่ถกเถียงได้** จึงทำ `kind` ที่สามด้วยตรรกะเดียวกับ `protocol` คือ "พบอะไร" เติมอย่างซื่อสัตย์ไม่ได้กับงานที่ไม่มีสิ่งที่พบ · ป้ายของ `argument` **บอกด้วยว่าข้อเสนอเป็นของผู้เขียน ไม่ใช่จุดยืนของศูนย์ฯ** ซึ่งเป็นปัญหาคู่ขนานกับกติกาข้อ 5 คนละด้าน (ข้อ 5 กันไม่ให้ศูนย์ฯ ดูเหมือนตัดสินใคร · ข้อนี้กันไม่ให้ศูนย์ฯ ดูเหมือนประกาศจุดยืนทางปรัชญาแทนผู้เขียน) |
| Security header สี่ตัว | #32 | ยิงดู header ของโดเมนจริงตามข้อ 8.3 ของรายงานตรวจอีกทีม แล้วพบว่า **เว็บ production ส่งออกตัวเดียวคือ HSTS ที่ Vercel ใส่ให้เอง** ขณะที่เว็บ staging ที่กำลังจะเลิกใช้ตั้งไว้ครบกว่า — กลับหัวกลับหาง · เพิ่ม `X-Content-Type-Options` · `Referrer-Policy` · `X-Frame-Options` · `Permissions-Policy` ใน `next.config.ts` · **จงใจไม่แตะ HSTS** เพราะ `includeSubDomains` บังคับ HTTPS กับโดเมนย่อยทุกตัวรวมถึงที่ยังไม่ได้สร้าง และ `preload` ถอนคืนใช้เวลาเป็นเดือน · **ยังไม่ใส่ CSP** เพราะต้องไล่ทุกโดเมนที่เว็บเรียกจริง (YouTube embed · Formspree · Vercel Analytics · โฮสต์รูปหลักสูตรวัฒนธรรม) แล้วทดสอบทุกหน้าที่มี embed ก่อน — ควรเป็นงานแยกที่มีการทดสอบของตัวเอง · ตรวจด้วยการยก `next start` ขึ้นมาจริงแล้วยิงดู header ที่เสิร์ฟออกมา |
| `check:content` + คำบรรยายขาดกลางคำ 56 หน้า | #33 | ย้ายตาข่ายตรวจเนื้อหาจากคลัง `comminno-web` ที่กำลังเลิกใช้เข้ามาเป็น `scripts/check-content.ts` แล้วใช้ไล่รายการตรวจ 34 ข้อกับเว็บจริง — เจอของจริงทันที: **`description` ของหน้าบทสรุปงานวิจัย 56 จาก 70 หน้าถูกตัดกลางคำ** ด้วย `.slice(0, 160)` ตรงๆ · ไม่มีใครเห็นเพราะไม่ได้อยู่บนหน้าเว็บ แต่ไปโผล่ใน**ผลการค้นหาและการ์ดตอนแชร์** ซึ่งเป็นจุดที่คนเห็นเว็บนี้ครั้งแรก · แก้ด้วย `truncate()` ใน `src/lib/text.ts` ที่ตัดตามขอบเขตคำจริงด้วย `Intl.Segmenter` · **บทเรียน: ตัวตรวจรุ่นแรกอ่านผลลัพธ์แล้วเดาจากเครื่องหมายวรรคตอนท้ายข้อความ ซึ่งผิดสำหรับภาษาไทย** (ไทยไม่มีจุดจบประโยค และคำไทยจำนวนมากลงท้ายด้วยวรรณยุกต์ตามปกติ) จึงฟ้อง 26 หน้าที่ไม่ได้มีปัญหา — เปลี่ยนไปตรวจที่ซอร์สว่ามีการเรียก `.slice()` กับ `description` หรือไม่ ซึ่งชี้ชัดและไม่มีผลบวกลวง · แก้ชื่อสองคนที่พบระหว่างทาง (ธาวิน · ไวพจน์) |
| `PageHero` จัดหัวหน้าหลักใหม่ 14 หน้า | #34 | **วินิจฉัยว่า "ทำหน้าให้ว้าว" เป็นปัญหาการจัดวาง ไม่ใช่ปัญหาการตกแต่ง** — แบนเนอร์สูง 526px (58% ของจอ) ดันเนื้อหาจริงลงใต้เส้นพับทั้งเจ็ดหน้าหลัก · ต้นเหตุคือภาพชุด paper-craft เจนมาโดย**เว้นพื้นครีมฝั่งซ้ายไว้ให้ข้อความ** แต่เว็บกลับเอาข้อความไปวางเหนือภาพ ทิ้งที่ว่างนั้นแล้วกินความสูงซ้ำอีกชุด · เปลี่ยนเป็นสองคอลัมน์แบบเดียวกับ hero หน้าแรก: ภาพ 526 → 386px · เนื้อหาจริงเริ่มที่ 913–1341px → **598–779px** ครบทุกหน้า · **จุดครอปวัดจากจุดศูนย์ถ่วงของพิกเซลที่อิ่มสี ไม่ได้กะด้วยตา** (ค่าที่กะเองรอบแรกดันวัตถุไปติดขอบเฟรม) เก็บเป็น field `focus` ใน `pageBanners.ts` · ลบ `PageBanner` ทิ้ง · ไม่แตะสี ฟอนต์ หรือตัวไฟล์ภาพ |
| ป้ายเหนือหัวเรื่องหน้าข่าว | #35 | ไทย "ข่าวและกิจกรรม" · อังกฤษ "News & Events" — หน้าข่าวเป็นหน้าเดียวใน 7 หน้าหลักที่ไม่มีป้ายนี้ ตอนย้ายมา `PageHero` จึงปล่อยว่างไว้ **เพราะไม่แต่งข้อความที่ผู้อ่านเห็นเอง** ต้องให้ผู้ใช้ตัดสิน · ผู้ใช้เลือกคงหัวเรื่องเดิมไว้แม้จะเป็นข้อความเดียวกับป้าย |
| `SectionIcon` รอบแรก 14 แบบ | #36 | **ผูกไอคอนกับบทบาทของหัวข้อ ไม่ใช่กับถ้อยคำ** — สำรวจแล้วพบหัวเรื่อง 504 จุด เขียนต่างกัน 128 แบบ แต่ทำหน้าที่ซ้ำกัน จัดตามบทบาทเหลือ **20 บทบาท** (คำตอบของคำถาม "ควรมีกี่แบบ") รอบนี้ทำ 14 ตัวแรก ครอบคลุม 93% ของจุดที่ใช้ · ภาษาภาพเดียวกับ `ServiceIcon` ตาม BRAND.md E1 · คู่สีบอกกลุ่ม ใช้คู่เดียวกับสี่ช่วงกระบวนการที่มีอยู่ **ไม่เพิ่มสีใหม่แม้แต่สีเดียว** · prop `onDark` สลับ facet เป็นสีครีมบนแถบ CTA พื้นเข้ม เพราะเงาต้องเป็นสีของกระดาษแผ่นนั้นเอง · **ตั้งใจไม่ใส่ 65 จุด — ชื่อข่าว ชื่อรายการสื่อ ปีที่ตีพิมพ์ เป็นเนื้อหา ไม่ใช่โครงสร้าง** |
| `SectionIcon` รอบสอง 6 แบบ ครบ 20 | #37 | คน · พันธมิตร · เรื่องราว · พันธกิจ · ฟอร์มติดต่อ · ขั้นตอนถัดไป · กลุ่มคน/องค์กร/ตัวตนใช้คู่สีแดง-เหลือง ซึ่งเป็นคู่เดียวกับช่วง "ส่งต่อความรู้" ทั้งสองรอบจึงไม่เพิ่มสีใหม่เข้าระบบเลย |
| ลบ `HomeLeadership.tsx` | #38 | ตรวจแล้วว่าทั้งคลังไม่มีอะไรอ้างถึง · `git log -S` แสดงว่าเคยถูกใช้บนหน้าแรกแล้วตายตอน Phase 3 สร้างหน้าแรกใหม่บนดีไซน์ซิสเท็ม · **บทเรียน: เกือบรายงานว่าป้าย "Leadership" ภาษาอังกฤษบนหน้าไทยเป็นบั๊กที่ขึ้นเว็บจริง — แต่ไฟล์นั้นไม่มีหน้าไหน render และคำว่า Leadership ที่พบบนหน้าแรกไทยอยู่ในพาดหัวข่าวซึ่งเป็นชื่อโครงการ (อนุญาตตาม BRAND.md PART C)** · แก้ผังโฟลเดอร์ใน CLAUDE.md ให้ตรงของจริงไปพร้อมกัน |
| แก้รายชื่อทีมงาน | #39 | ผู้ช่วยของผู้ใช้ตรวจรายชื่อมาให้ — ตัดผู้ช่วยวิจัยที่ไม่ได้อยู่กับศูนย์ฯ แล้ว 4 คน (**ลบไฟล์ภาพออกจาก `public/` ด้วย** ไม่ใช่แค่ลบการ์ด) · แก้ Dr. Watsayut Kongchan → **Dr. Wassayut Kongjan** ซึ่งเขียนซ้ำอยู่ใน `media.ts` อีกสามจุดที่ผู้อ่านเห็น · เอกสิทธิ์ สุมนา → **เอกะสิทธิ์ สุมะนะ** · สุพัตรา เพ็ชรี → **สุพัตรา เพชรี** · **บทเรียนที่ต้องจำ: ข้อมูลที่ผู้ช่วยส่งมาขัดกับที่ผู้ใช้พิมพ์ยืนยันเองเมื่อไม่กี่ชั่วโมงก่อนสองจุด** (ไวพจน์ · ธาวิน) จึงหยุดถามก่อน ไม่ย้อนค่าเอง — ปรากฏว่าคำตอบต่างกันคนละทาง (ไวพจน์คงตามผู้ใช้ · ธาวินตัดออกทั้งคน) เดาทางใดทางหนึ่งก็ผิดครึ่งหนึ่ง · **รายการอ้างอิงไม่แก้ตาม** เพราะบทความที่ตีพิมพ์แล้วลงชื่อ Watsayut Kongchan ทั้งใน Crossref และในวารสาร การอ้างอิงต้องตรงกับต้นฉบับ ไม่ใช่ตรงกับชื่อที่เจ้าตัวใช้วันนี้ (เขียนกำกับไว้สามที่ + อธิบายใน `check-content.ts` ว่าทำไมชื่อนี้ถึงไม่อยู่ในลิสต์ค่าที่เลิกใช้) |

| วิดีโอพื้นหลัง CTA (ชุด H) | #41 | **งานค้างชิ้นสุดท้ายของ Phase 2** · คลิป 8 วินาทีวนต่อสนิท เจนจาก `cta-bg.webp` เอง · **รอบแรกเสียเงินฟรีเพราะโมเดลวาดฉากใหม่แทนที่จะขยับภาพเดิม** (ความสว่างเฉลี่ย 23.6 → 54.2 · เส้นด้ายกลายเป็นสามเส้น · เพี้ยนตั้งแต่วินาทีที่ 0.8) ต้นเหตุคือ prompt enhancement ของ Google ที่**ปิดไม่ได้จริง** (ส่ง `enhance_prompt: false` แล้ว API ตอบว่าไม่รู้จักพารามิเตอร์) · **ตัวแก้คือใส่ภาพเดิมเป็น `end_frame` ด้วย** บังคับให้คลิปจบที่ภาพเดิมเป๊ะ + `negative_prompt` ซึ่งใช้ได้จริง · **ผลพลอยได้: เฟรมแรกกับสุดท้ายต่างกัน 1.00 จึงวนต่อสนิทอยู่แล้ว ไม่ต้องต่อกลับหลังแบบ hero** ไฟล์เล็กลงครึ่งหนึ่ง · `CtaBackdrop` โหลดคลิปเมื่อเลื่อนมาใกล้เท่านั้นเพราะแถบนี้อยู่ท้ายหน้า และหยุดเล่นเมื่อพ้นจอ · `reached` เป็นสลักทางเดียว เพราะรุ่นแรกถอด `<video>` ออกจาก DOM ทุกครั้งที่เลื่อนพ้น ทำให้คลิปเริ่มใหม่ทุกรอบ (เจอจากการตรวจด้วยเบราว์เซอร์จริง ไม่ใช่จากการอ่านโค้ด) |
| บทสรุปงานวิจัยกลุ่ม ก สองชิ้นแรก | #42 | รวม 37 จาก 75 · **ระบุกลุ่ม ก ได้ครบด้วย Unpaywall** — ตรวจ 23 รายการที่มี DOI แล้วได้ open access 10 ชิ้นพอดี ตรงกับที่สำรวจไว้ · **ดาวน์โหลดตรงถูกบล็อกทั้ง 10 ชิ้น** (T&F 403 · Elsevier 403 · hrcak 418 · Dovepress 404) ไม่ไล่หลบระบบกันบอต · ได้มาสองชิ้นผ่าน **PubMed Central** และ **DSpace REST API ของมหาวิทยาลัยรัฐเซนต์ปีเตอร์สเบิร์ก** ซึ่งเป็น API สาธารณะที่เปิดให้ใช้ตามปกติ · ตัดชื่อแอปห้าแอปและเอเจนซีสองแห่งออกตามกติกาข้อ 5 · **เพิ่ม `"ru"` เข้า `articleLanguage`** เพราะบทความหนึ่งตีพิมพ์เป็นภาษารัสเซีย |
| อีเมลที่หายจากสี่หน้า + ตรวจลิงก์เปล่า | #43 | **คำเตือน lint เรื่อง `EMAIL` ที่ import แล้วไม่ได้ใช้ ไม่ใช่ import เกิน แต่เป็นอาการของข้อความในลิงก์ที่หายไป** — บน production มี `<a href="mailto:…"></a>` เปล่าหกจุด ที่อยู่อีเมลของศูนย์ฯ จึงไม่ปรากฏบน `/collaborate` และ `/privacy-policy` ทั้งสองภาษาเลย (ที่เห็นมาจาก Footer เท่านั้น) · หน้านโยบายหนักสุดเพราะ "ใช้สิทธิเหล่านี้ได้โดยเขียนมาที่ ___" ชี้ไปที่ว่างเปล่า · **TypeScript ไม่ฟ้องเพราะ JSX ยอมให้ element ว่างได้** เสียงเดียวที่ดังคือคำเตือน eslint ซึ่งอ่านผ่านๆ เหมือน import เกิน จึงถูกข้ามมาหลายรอบ · เพิ่มการตรวจ (3ข) ใน `check-content.ts` แล้วทดสอบกับ build ก่อนแก้ว่าจับได้ครบหกจุดพอดี |
| CSP header | #44 | ปิดงานค้างตัวสุดท้ายจาก PR #32 · **รายการโดเมนมาจากการวัดของจริง ไม่ใช่การไล่อ่านซอร์ส** — ยก `next start` แล้วเปิดหกหน้าที่มี embed/ฟอร์มด้วยเบราว์เซอร์จริง ดัก `securitypolicyviolation` · **GA4 เกือบหลุด** เพราะโหลดเฉพาะเมื่อผู้ใช้กดยอมรับ จึงไม่โผล่ในการทดสอบอัตโนมัติ ต้องใส่จากการอ่าน `AnalyticsConsent.tsx` (ลืม = แถบคุกกี้ทำงานแต่ GA ไม่เก็บอะไรเลยแบบเงียบๆ) · **ทดสอบเชิงลบด้วยการตัดสามโดเมนออกแล้วดูว่า violation โผล่ตรงตำแหน่งที่ควรจริง** ซึ่งเป็นตัวแยกว่า "ต่อไม่ติดเพราะเน็ต" กับ "ถูก CSP บล็อก" · **ยอมรับ `'unsafe-inline'` ใน script-src อย่างรู้ตัว** เพราะ nonce บังคับให้ทุกหน้ากลายเป็น dynamic ซึ่งแลกทั้งเว็บ 183 หน้าไปเพื่อ CSP ที่แข็งขึ้นหนึ่งขั้น ไม่คุ้ม · `check:csp` **จงใจไม่อยู่ใน verify** เพราะต้องยกเซิร์ฟเวอร์และเปิดเบราว์เซอร์ · playwright ก็ไม่ใส่เป็น dependency ด้วยเหตุผลเดียวกัน |
| บทสรุปงานวิจัยกลุ่ม ก อีก 5 ชิ้น | #45 | รวม 42 จาก 75 · **ไฟล์มาจากผู้ใช้ดาวน์โหลดผ่านเบราว์เซอร์** ซึ่งเป็นทางออกที่ถูกต้องสำหรับสำนักพิมพ์ที่กันบอต ไม่ใช่การหลบ · ทั้งห้าเป็น CC จึงเก็บใน `research-sources/papers/` ได้ · **บทความขัดกันเองให้เขียนบอก ไม่ใช่เลือกข้างเงียบๆ** — งานไข้เลือดออกมีสถิติสรุปที่บอกว่าพฤติกรรมปี 2558 สูงสุด ขณะที่ตารางความถี่แสดงว่าการทำทุกสัปดาห์ลดลง บทสรุปยกตารางความถี่แล้วระบุใน caveat ว่าทำไม · **ผลที่ค้านสมมติฐานเขียนตรงตามนั้น** (H1 ของงานปากต่อปากเชิงลบไม่ผ่าน · H2 ไปคนละทางกับที่ทำนาย) · ระบุชื่อยูทูบเบอร์ในงานอินฟลูเอนเซอร์เกมเพราะเป็นขอบเขต ไม่ใช่เป้าของข้อวิจารณ์ (ข้อยกเว้นของกติกาข้อ 5) · Crossref ไม่ได้ลงเลขบทความให้ Cogent Social Sciences 2025 เติมผ่าน `CITATION_FIXES` โดยอ้างบรรทัด "To cite this article" ในไฟล์ · **ภาพหนึ่งใบไม่ตรงแนวคิด (แปดแท่งแทนหก) alt เขียนตามที่เห็นจริง ไม่ใช่ตามที่สั่ง** และแจ้งผู้ใช้ให้ตัดสินว่าจะเจนใหม่ไหม |
| เจนภาพไข้เลือดออกใหม่ | #46 | ผู้ใช้อนุมัติเจนใหม่ · รอบสองใช้แนวคิดเดิมและสั่งให้นับหกแท่งอย่างชัดเจน ได้**เจ็ดแท่ง** — **บทเรียน: gemini-3-pro-image นับวัตถุเกินสี่ชิ้นไม่แม่น การเน้นคำสั่งไม่ช่วย ต้องเปลี่ยนแนวคิดให้นับน้อยลงแทน** · รอบสามใช้ "สามแท่งไต่ขึ้น + หนึ่งแท่งล้ม" (สี่ชิ้น) ตรงพอดี · ก่อนใช้เงินรอบสามหยุดถามผู้ใช้พร้อมส่งภาพรอบสองให้เทียบ ไม่เจนซ้ำเองแม้ราคาถูก เพราะทุกรอบคิดเงินและการอนุมัติครั้งก่อนครอบคลุมแค่หนึ่งรอบ · ไม่แจ้ง IndexNow เพราะเปลี่ยนเฉพาะภาพ |
| บทสรุปงานวิจัยชุดที่ 3 (โควิด ABS · TENCON · วิดีโอความปลอดภัย JPHD) | #47 | รวม 45 จาก 75 · **สองชิ้นสงวนลิขสิทธิ์ (SAGE · IEEE) เขียนสรุปจากไฟล์ที่ผู้ใช้ส่งมาได้ แต่ไม่เก็บไฟล์ ไม่ใส่ `license`/`localCopy`/`pdfUrl` และหน้าเว็บบอกผู้อ่านว่าไม่มีสำเนา** — การอ่านเพื่อสรุปทำได้กับทุกบทความ สิ่งที่ห้ามคือเผยแพร่ซ้ำ · **JPHD 2021 ไม่มี DOI** ต่างจากบทความปี 2022 ของวารสารเดียวกัน จึงผูกผ่าน `THAIJO_SOURCES` แล้วรันสคริปต์ใหม่ (self → index) · ไฟล์ที่ผู้ใช้ส่งมาเป็นภาพสแกนอ่านข้อความไม่ได้ ดึงไฟล์ทางการจาก ThaiJO แทน และตรวจสัญญาอนุญาตจากหน้านโยบายวารสาร (CC BY-NC) เพราะหน้าบทความไม่มีลิงก์ CC · **บทสรุปโควิดตัดชื่อรัฐมนตรี ผู้นำรัฐบาล ยี่ห้อวัคซีน และบริษัทออกทั้งหมดตามกติกาข้อ 5** คงไว้แต่ตัวเลขสาธารณะ · บทสรุป TENCON ระบุตามบทความว่าผู้วิจัยหลักเป็นผู้สอนเองและไม่มีกลุ่มควบคุม · ภาพสามใบตรงแนวคิดตั้งแต่รอบแรกเพราะทุกใบนับวัตถุไม่เกินสามชิ้น (บทเรียนจาก #46) |
| วิดีโอเล่าสาระหลักของบทความ นำร่อง 3 เรื่อง | #49 | ไข้เลือดออก · นิทรรศการช้าง · อนิเมะ × ไทย/อังกฤษ ผ่าน 3 ฉบับ · ผู้ใช้ทักสองเรื่องที่กลายเป็นกติกาถาวร: **ตัดบรรทัดไทยตามวรรค** (เจอ "คนรู้ \| ลึก" บนจอจริง) และ **ศูนย์ฯ เป็นผู้เล่า ไม่ใช่ผู้ทำ** (ประธานของกริยาวิจัยต้องเป็นทีมวิจัย) · การ์ดปิดใส่ชื่อผู้เขียนทุกคนจาก `citation.authors` · โค้ดอยู่ `scripts/video/` ไฟล์สื่อไม่ขึ้นคลัง · กติกาเต็มอยู่หัวข้อ "วิดีโอเล่าสาระหลักของบทความ" ท้ายไฟล์ |
| ฝังวิดีโอบนหน้า `/research/[slug]` | #50 | `paperVideos.ts` (slug → id YouTube ไทย/อังกฤษ) · `PaperVideo` facade โหลด iframe เมื่อกดเล่น หน้าปกคือภาพประจำบทความในเว็บเรา · `VideoObject` ใน JSON-LD เฉพาะหน้าที่มีคลิป · `check:content` ตรวจ slug · **คลิปต้อง public/unlisted** private ฝังไม่ได้ · ระบุ id จาก oEmbed + thumbnail เพราะหน้า watch กันบอต ไม่ไล่หลบ |
| วิดีโอบนหน้าแรก + แก้ id คลิปที่ถูกลบ | #51 | ผู้ใช้ลบคลิปไข้เลือดออกไทย*ตัวที่ฝังไว้*แทนตัวซ้ำ → เปลี่ยน id · ส่วน "วิดีโอความรู้" บนหน้าแรกอ่านจาก `homeVideos(locale)` หน้าไทยได้คลิปไทย หน้าอังกฤษได้คลิปอังกฤษ · `VideoShowcase` รับ `videos` prop + poster srcset + ลิงก์ไปบทสรุป |
| วิดีโอชุดที่ 2 อีก 5 เรื่อง | #52 | อ้างอิงจากแชตบอต · วิดีโอ 360 · ภาพไทยหลังปลดล็อกกัญชา · รีวิวลบกับโฆษณาเปรียบเทียบ · ออกแบบร่วมกับแรงงานข้ามชาติ × ไทย/อังกฤษ = 10 คลิป · สตอรี่บอร์ดแยกไฟล์ `storyboard_batch2.py` · ค่าใช้จ่ายจริง ภาพ 30 ใบ $6.00 + เสียง 80 ท่อน $2.67 = **$8.67** (ประเมิน $8.08 — เสียงคิดตามอักขระจริง แพงกว่าประเมิน 28%) · **บทเรียน**: (1) ช่อง `url` ในผลสถานะของ ElevenLabs คือ WebP ตัวอย่าง ต้องใช้ `master_url` ถึงได้ PNG เต็ม (2) ประโยคปิดที่ข้อความซ้ำกันทุกเรื่องทำให้ตัวจับคู่ด้วยข้อความยุบ 5 ไฟล์เหลือ 1 เงียบๆ → `fetch_segments.py --sessions` ใช้แผนที่ session ที่จดตอนสั่งเจน (3) ภาพเกินบรีฟสองใบ (เม็ดยา 4 แทน 3 · เศษกระดาษข้างโดม) ลบด้วย `patch_bg.py` สังเคราะห์พื้นจากบริเวณรอบๆ แทนเจนใหม่ — **สีพื้นต้องอ้างเฉพาะที่** เพราะมุมล่างของภาพมืดกว่า #F7F6F2 พอสมควร เทียบค่าตายตัวจะมองพื้นเป็นวัตถุทั้งแผ่น (รอบแรกจึงไม่ลบอะไรเลย) (4) ดอกจัน 3 ใน 5 ใบมี 7–8 แขนแทน 6 ยังอ่านเป็นดอกจันได้ ไม่เจนใหม่ (5) เฟรมที่ประกอบแล้วยังมี "รีวิวลบ 1 \| ข้อความ" · "4 \| weeks," · "comparative or \| not" — ตารางลักษณนามหลังเลขต้องมีคำของชุดใหม่ (ข้อความ คลิป ท่า ชั่วโมง องศา + ชื่อเดือน) · เทียบคำอังกฤษต้องตัดจุลภาคท้ายคำก่อน · เพิ่มกันคำอังกฤษโดดคำเดียวบรรทัดสุดท้าย (ดึงคำท้ายบรรทัดก่อนลงมาคู่) · **ประกอบเสร็จแล้วต้องรัน `check_wrap2.py` ซ้ำและอ่านทุกบรรทัด ไม่ใช่ดูแค่ `INTRA: 0`** เพราะการตัดที่ช่องว่างผ่านตัวตรวจแต่ยังผิดกติกาเลข-ลักษณนามได้ · วิดีโอความปลอดภัยบนเครื่องบิน JPHD 2021 ถูกสลับออกเพราะทะเบียนยังไม่มี `citation` การ์ดปิดจึงใส่ชื่อผู้เขียนไม่ได้ · ลบ `.pyc` ที่หลุดเข้าคลังใน #49 และเพิ่ม `__pycache__/` ใน `.gitignore` |

**ค้างฝั่งผู้ใช้ (ไม่ใช่งานโค้ด):** เพิกถอน PAT ของ Grok · ลบ branch `grok/visual-assets` · เพิ่ม `en.cominnocenter.com` ใน Vercel เป็น redirect 308 → www แล้วแก้ CNAME · ยกเลิก **เฉพาะ website plan** ของ Wix ราวกลางเดือนกันยายน 2569 (ห้ามแตะโดเมน/DNS — MX ของ Google Workspace อยู่ที่นั่น) · ชวน ผศ.ดร.ธีรดา ผูก ORCID เข้ากับ Crossref/Scopus เพื่อให้ผลงานเลื่อนขึ้นเป็นระดับ `doi` อัตโนมัติในรอบรันถัดไป

**ค้างฝั่งงานเว็บ:** ไม่มีแล้ว — วิดีโอ hero (ชุด D) และวิดีโอพื้นหลัง CTA (ชุด H) เสร็จทั้งคู่ · CSP ใส่แล้วใน PR #44 · เหลือเฉพาะบทสรุปงานวิจัยซึ่งติดที่ไฟล์บทความ ดูหัวข้อถัดไป

**บทสรุปงานวิจัยที่เหลืออีก 30 ชิ้น — คอขวดคือไฟล์ ไม่ใช่เวลาเขียน** (ตรวจสถานะ open access ของทุกชิ้นที่มี DOI ใหม่ด้วย Unpaywall 4 ก.ย. 2569 · รายการเต็มพร้อม DOI ส่งให้ผู้ใช้ในแชทวันเดียวกัน)

ตรวจแล้วว่า **ดึงไฟล์เองอัตโนมัติไม่ได้อีกแม้แต่ชิ้นเดียว** แบ่งได้ดังนี้

| กลุ่ม | จำนวน | ติดตรงไหน |
|---|---|---|
| CC แต่สำนักพิมพ์กันบอต (กลุ่ม ก) | 3 | Unpaywall ยืนยันว่ากลุ่มนี้มี 10 ชิ้นพอดี · ได้มาแล้ว 7 (2 ผ่าน API สาธารณะใน PR #42 · 5 ผู้ใช้ดาวน์โหลดจากเบราว์เซอร์ส่งมา) · **เหลือ 3 ที่รอผู้ใช้ส่ง**: In Defense of AVE (`10.18848/2470-9247/cgp/a140`) · หนังสือ Philosophical Foundations (`10.1007/978-981-97-1013-3`) · Bayesian inference ใน WACANA (`10.32509/wacana.v23i1.3388`) · **ห้ามไล่หลบระบบกันบอต** เพราะผิดเงื่อนไขการใช้งานของเขา · ไฟล์ CC **เอาลงคลัง GitHub ได้** |
| ปิด ต้องใช้สิทธิ์ห้องสมุดจุฬาฯ | 11 | ผู้ใช้ส่งไฟล์มาให้อ่านได้ (ทำแล้วกับ ABS 2022 · TENCON 2023) แต่**ห้ามลงคลัง** และหน้าเว็บไม่ใส่ `license`/`localCopy`/`pdfUrl` · ในนี้มี**หนังสือทั้งเล่ม 2 เล่ม** (Global Communication 2025 · Communication and Libertarianism 2021) ซึ่งต้องตกลงรูปแบบบทสรุปก่อนทำ |
| ไม่มี DOI และไม่มีลิงก์ | 13 | ต้องใช้ไฟล์ของผู้เขียนเอง (ส่วนใหญ่ของ ผศ.ดร.ธีรดา) · ถ้าบทความอยู่บน ThaiJO ให้ผูกผ่าน `THAIJO_SOURCES` ในสคริปต์แล้วรันใหม่ เหมือนที่ทำกับ JPHD 2021 |
| มีลิงก์ Semantic Scholar แต่ไม่มีไฟล์เปิด | 3 | ต้องใช้ไฟล์ของผู้เขียนเอง |

**ทางออกที่ตกลงกับผู้ใช้แล้ว:** ผู้ใช้จัดโฟลเดอร์ Google Drive แล้วแชร์แบบ "ผู้ที่มีลิงก์ — ผู้อ่าน"
ส่งลิงก์มาให้อ่าน · รายการพร้อมลิงก์ตรงทุกชิ้นส่งให้ผู้ใช้ไปแล้ว (ไฟล์ `รายการบทความเข้า-drive.md`)
· **การอ่านบทความเพื่อเขียนสรุปด้วยคำของเราเองทำได้กับทุกบทความไม่ว่าลิขสิทธิ์เป็นของใคร
สิ่งที่ทำไม่ได้คือเผยแพร่ตัวไฟล์ซ้ำ**

### คลังบทความเต็มบน Google Drive (4 ก.ย. 2569)

ผู้ใช้ต้องการให้ไฟล์บทความทุกชิ้นอยู่ที่เดียวและให้ Claude อ่านได้เองโดยไม่ต้องส่งซ้ำ เพราะจะใช้ทำ**วิดีโอเล่าสาระหลักของบทความ**ต่อจากบทสรุป

- โฟลเดอร์ `ComInno — คลังบทความเต็มสำหรับสื่อสารงานวิจัย` ใน Drive ของผู้ใช้ (id `125T6Rh4NUi1uljJraKM3tSeMWIU2sb4t`) เข้าถึงผ่านตัวเชื่อม Google Drive ของบัญชีผู้ใช้ **ไม่ได้แชร์สาธารณะ** — สี่โฟลเดอร์ย่อย: `01` CC (สำเนาเดียวกับ `research-sources/papers/`) · `02` สงวนลิขสิทธิ์ ใช้ภายใน **ห้ามลง GitHub** · `03` ข้อความสกัด `.txt` ชื่อไฟล์ = slug · `04` กล่องรับไฟล์ใหม่ที่ผู้ใช้วางให้อ่าน
- **อ่านจากโฟลเดอร์ 03 (txt) ก่อนเสมอ** `read_file_content` กับไฟล์ข้อความถูกกว่า PDF หลายสิบเท่า (PDF ผ่านตัวเชื่อมกลับมาเป็น base64) · ถ้าเจอไฟล์ใหม่ในโฟลเดอร์ 04 ให้สกัดข้อความ เปลี่ยนชื่อเป็น slug แล้วบันทึกสถานะลง Google Sheet `ดัชนีผลงาน 75 ชิ้น` ในโฟลเดอร์เดียวกัน
- **ตัวเชื่อม Drive ส่งไบนารีขึ้นไม่ได้ในทางปฏิบัติ** (`create_file` รับ base64 แต่ PDF หนึ่งไฟล์กินโทเค็นเป็นแสน) — ไฟล์ PDF จึงส่งเป็น zip ให้ผู้ใช้ลากเข้าเอง ส่วน txt/CSV/Doc สร้างตรงได้ · ไฟล์ CC ยังต้องอยู่ใน `research-sources/papers/` ของคลังนี้ต่อไปเพราะ `check:citations` ตรวจ `localCopy`
- การดึงไฟล์กลับจากแหล่งทางการทำได้เฉพาะ ThaiJO (`citation_pdf_url`) · Europe PMC · โฮสต์ที่ให้ PDF ตรง — **ที่ดึงไม่ได้**: SAGE/T&F/Elsevier (กันบอต) · Tripodos (captcha) · `iafor.org`/`iises.net` (ไม่อยู่ในรายการโฮสต์ที่เซสชันนี้ออกเน็ตได้) · `digital.car.chula.ac.th` (403) · DSpace SPbU ย้ายระบบ · JHR ฉบับก่อน 2018 กับ CMAP ไม่มีสำเนาออนไลน์ — พวกนี้ผู้ใช้ดาวน์โหลดจากเบราว์เซอร์แล้ววางโฟลเดอร์ 04

### วิธีทำวิดีโอประกอบเว็บ (30 ส.ค. 2569)

เซสชัน Claude มีตัวเจนวิดีโอแล้วผ่าน ElevenLabs MCP (`creative_*`) ไม่ต้องพึ่ง Grok เหมือนตอนทำภาพนิ่ง

**บทเรียนสำคัญ: อย่าสั่งด้วยคำบรรยายล้วน** ลองสั่ง "seen from above" ตรงๆ แล้ว Veo ไม่ทำตาม ได้มุมเฉียงกลับมาทุกครั้ง (ปิด prompt enhancement ของ Google ไม่ได้ โมเดลไม่เปิดพารามิเตอร์นั้นให้ตั้ง) **วิธีที่ได้ผลคือใช้ภาพจริงบนเว็บเป็น `start_frame`** แล้วให้โมเดลขยับต่อ — มุมกล้อง สี และองค์ประกอบจะตรงกับเว็บโดยอัตโนมัติเพราะเป็นภาพเดียวกัน (`end_frame` ก็มี ใช้เมื่ออยากให้คลิปไป*จบ*ที่ภาพนั้น)

ขั้นตอน: `creative_create_flow` → `creative_create_asset_upload` + PUT ไฟล์ → `creative_finalize_asset_upload(flow_id)` ได้ node_id → `creative_add_flow_node` (veo-3.1-fast-generate-001, `generate_audio: false`) → `creative_connect_flow_nodes` ต่อเข้าพอร์ต `start_frame` → `creative_run_flow_nodes`

ข้อจำกัดที่ต้องรู้: Veo รับแค่ 16:9 หรือ 9:16 (ภาพ 2.33:1 ของเราต้องเติมพื้นครีม `#F7F6F2` บน-ล่างก่อน) · คลิปละ 4/6/8 วินาทีเท่านั้น · **ราคาคิดทุกครั้งที่กดสร้าง ไม่มีการลองฟรี** ใช้ `estimate_only: true` ดูราคาก่อนเสมอ (8 วิ 1080p ≈ $0.80) และต้องขออนุมัติผู้ใช้ก่อนกดทุกครั้ง

เครื่องนี้ไม่มี ffmpeg มาให้ ติดตั้งด้วย `pip install imageio-ffmpeg` แล้วเรียกจาก `imageio_ffmpeg.get_ffmpeg_exe()`

### วิดีโอเล่าสาระหลักของบทความ — กติกาที่ตกลงกับผู้ใช้แล้ว (4 ก.ย. 2569)

นำร่อง 3 เรื่อง × ไทย/อังกฤษ (ไข้เลือดออก · นิทรรศการช้าง · อนิเมะ) ผ่านมา 3 ฉบับจนผู้ใช้รับคุณภาพเสียงและภาพแล้ว
ตัวประกอบอยู่ใน `scripts/video/` (Python + PIL + imageio-ffmpeg · อ่าน README ในนั้น) ส่วนไฟล์สื่อ (ภาพฉาก · เสียงพากย์ทีละย่อหน้า ·
จิงเกิล/เพลง/SFX · คลิปที่ประกอบแล้ว) **ไม่อยู่ในคลังนี้** เพราะคลังเป็นสาธารณะและมีเสียงของผู้ใช้เอง — ส่งเป็น zip ให้ผู้ใช้เก็บแล้ว (4 ก.ย.)

- **ศูนย์ฯ เป็นผู้เล่า ไม่ใช่ผู้ทำ** — ผู้ใช้ทักว่า "ศูนย์จัดนิทรรศการ / ศูนย์ทำการศึกษา" เป็นการเคลมงานมาไว้ที่ศูนย์ฯ ทั้งที่ผู้ร่วมวิจัยเป็นคนจัดจริง
  กริยาที่เป็นการ*ทำ*วิจัย (สำรวจ จัด สัมภาษณ์ เก็บข้อมูล วิเคราะห์ ติดตาม พบว่า สรุปว่า · surveyed, staged, interviewed, found) ประธานต้องเป็น
  **ทีมวิจัย / คณะผู้วิจัย / ผู้วิจัย** (the research team / the researchers / the authors) · กริยาที่เป็นการ*เล่า* (ขอเล่า สรุปให้ฟัง ชวนอ่าน · presents,
  brings you) จึงใช้ **ศูนย์คอมอินโน / เรา** ได้ · หน่วยงานอื่นที่ร่วมทำให้บอกบทบาทตรงๆ ("ร่วมกับกระทรวงสาธารณสุข") · งานเก่ากว่าศูนย์ฯ ห้ามพูดว่าศูนย์ฯ ทำ
  (ไข้เลือดออกเก็บข้อมูล 2556–2558)
- **งานที่ต่อยอดจากนิสิต** ให้บอกในบทด้วยถ้อยคำที่ผู้ใช้เลือก: "งานนี้**ต่อยอดจากผลงานวิจัยของนิสิต**คณะนิเทศศาสตร์ โดยมีอาจารย์ของศูนย์คอมอินโนเป็นที่ปรึกษา"
  (ผู้ใช้ยืนยันว่านับเป็นงานของศูนย์ฯ ได้ เพราะวิทยานิพนธ์/สารนิพนธ์/professional project/individual study เป็นลิขสิทธิ์ของจุฬาฯ และนิสิตเห็นชอบให้ต่อยอดเป็นบทความ)
- **การ์ดปิดต้องมีชื่อผู้เขียนบทความทุกคน** ตามลำดับในบทความ + ชื่อวารสาร + ปี — `credits.py` ดึงจาก `citation.authors` ใน `publications.ts`
  (ไม่ใช่ `authors` ซึ่งมีแต่คนของศูนย์ฯ) จึงตรงกับต้นฉบับเสมอ · ประโยคปิดที่พากย์ว่า "หนึ่งในงานวิจัยของศูนย์คอมอินโน" คงไว้ได้เพราะมีชื่อผู้เขียนอยู่บนจอเดียวกัน
- **ข้อความไทยบนจอต้องตัดบรรทัดตามวรรคก่อนเสมอ** — ตัวตัดคำ ICU มอง "รู้ลึก" เป็นสองคำ ผู้ใช้จึงเจอ "คนรู้ | ลึก" บนจอจริง · `build_v2.wrap()` ตัดตามช่องว่างก่อน
  ยอมลดฟอนต์ได้สองขั้นเพื่อรักษาวรรค และค่อยตัดในวรรคด้วย ICU เฉพาะวรรคที่ยาวเกินหนึ่งบรรทัด · เลขต้องไม่แยกจากลักษณนาม/คำเชื่อม ("23 คน" · "5 ใน 6" · "ราว 300")
  และวงเล็บสั้นๆ ต้องอยู่บรรทัดเดียว (`(r = 0.64)` เคยหลุด ")" ไปบรรทัดใหม่) · **เขียนข้อความบนจอให้มีวรรคตามหน่วยความหมาย** วรรคไหนยาวเกิน 30 อักขระให้แบ่งที่ต้นฉบับ
  ไม่ปล่อยให้ตัวตัดคำเดา · `check_wrap2.py` พิมพ์ทุกบรรทัดของทุกฉากและฟ้อง `INTRA` ถ้ามีการตัดในวรรค — รันก่อนประกอบทุกครั้ง
- **เสียง** พากย์ด้วยเสียงโคลนของผู้ใช้เอง (ไทย "Smith Boon" · อังกฤษ "Smith Boonbr" สำเนียงอังกฤษ ไม่ใช่สำเนียงไทย) ด้วย `eleven_v3` เท่านั้น —
  `multilingual_v2`/`turbo_v2_5` อ่านไทยเป็นภาษาอื่นเพราะตัวเชื่อมตั้ง `language_code` ไม่ได้ และ `eleven_v4` บัญชีนี้ใช้ไม่ได้ · "คอมอินโน" ทุกโมเดลอ่านเป็น "คอมอินโน่"
  ผู้ใช้ยอมรับแล้ว ไม่ต้องลองสะกดใหม่อีก · อัด**ทีละย่อหน้า** เพื่อคุมช่องหายใจเอง (0.8 วิ · ก่อนประโยคปิด 1.4 วิ) · แก้บทย่อหน้าไหนอัดใหม่เฉพาะย่อหน้านั้น (≈ $0.03–0.05 ต่อท่อน)
- **มาสเตอร์เสียง** highpass 80 Hz · ลด 250 Hz · เพิ่ม 4 kHz · compressor เบา · loudnorm −16 LUFS / TP −1.5 · เพลงพื้นหลัง −27 dB กดลงอัตโนมัติเมื่อมีเสียงพูด (sidechain) ·
  จิงเกิลเปิด −12 dB จางหมดที่ 8 วิ · ตรวจผลด้วยการวัด integrated loudness ของทุกคลิป ไม่ใช่ฟังอย่างเดียว
- **ทุกอย่างที่คิดเงินต้องประเมินราคาแล้วขออนุมัติก่อนกดทุกรอบ** การอนุมัติครั้งก่อนไม่ครอบคลุมรอบถัดไป (ผู้ใช้ย้ำไว้)

### IndexNow — แจ้ง Bing เมื่อมีหน้าใหม่ (31 ส.ค. 2569)

หลังเพิ่มโครงการ/ข่าวใหม่แล้ว merge ขึ้น production **ให้รันหนึ่งบรรทัด**:

```bash
npm run indexnow -- https://www.cominnocenter.com/impact/<slug>
```

Bing จะรู้ภายในไม่กี่นาทีแทนที่จะรอบอทมาคลานเจอเองเป็นสัปดาห์ — และเพราะ **ChatGPT search ดึงจากดัชนีของ Bing** ผลงานใหม่จึงไปโผล่ในคำตอบของ AI เร็วขึ้นด้วย (Yandex/Naver/Seznam ใช้มาตรฐานเดียวกัน · **Google ไม่ร่วม** ฝั่ง Google ยังใช้ sitemap + Search Console เหมือนเดิม)

**ข้อห้ามสำคัญ: อย่าใส่ในขั้นตอน build หรือรันทุก deploy** แนวปฏิบัติของ IndexNow คือแจ้งเฉพาะ URL ที่เนื้อหาเปลี่ยนจริง การยิงทั้งเว็บซ้ำๆ เข้าข่ายสแปมและอาจถูกจำกัดสิทธิ์ · `--all` มีไว้สำหรับการเปลี่ยนแปลงครั้งใหญ่เท่านั้น

กุญแจอยู่สองที่และ**ต้องตรงกันเสมอ**: ค่า `KEY` ใน `scripts/indexnow.mjs` กับชื่อ+เนื้อไฟล์ `public/<key>.txt` ถ้าไม่ตรงจะได้ HTTP 403 ทั้งหมด · ไฟล์กุญแจต้องขึ้น production ก่อนถึงจะส่งได้ (IndexNow จะมาดึงไปตรวจ)

**จุดที่ต้องหยุดหารือกับผู้ใช้:** เปลี่ยนสี/ฟอนต์นอก BRAND.md · เพิ่ม/ลบหน้าในเมนูหลัก · แตะ DNS หรือบัญชี Wix · เผยแพร่ข้อมูลบุคคลหรือผลงานวิชาการที่ยังตรวจไม่ครบ · build/deploy พัง
